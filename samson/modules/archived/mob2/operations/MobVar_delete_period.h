
/**
 File autogenerated with samsonModuleParser. Please, edit to complete this operation
 */

#ifndef _H_SAMSON_mob2_MobVar_delete_period
#define _H_SAMSON_mob2_MobVar_delete_period


#include <samson/module/samson.h>
#include <samson/modules/cdr/CellPos.h>
#include <samson/modules/mob2/TelMonth.h>



namespace samson{
namespace mob2{


class MobVar_delete_period : public samson::Map
{
	// Inputs - Outputs
	TelMonth telmonth;
	samson::cdr::CellPos cell;

public:


#ifdef INFO_COMMENT //Just to include a comment without conflicting anything
	// If interface changes and you do not recreate this file, consider updating this information (and of course, the module file)

	input: mob2.TelMonth cdr.CellPos
	output: mob2.TelMonth cdr.CellPos

	extendedHelp: 		Delete the period to calculate individual variables of a whole sample

#endif // de INFO_COMMENT

	void init(samson::KVWriter *writer )
	{
	}

	////// ****** Processing of all months as a period ****** //////

	void run(  samson::KVSetStruct* inputs , samson::KVWriter *writer )
	{
		for(uint64_t i=0; i<inputs[0].num_kvs; i++)
		{
			telmonth.parse(inputs[0].kvs[i]->key);
			cell.parse(inputs[0].kvs[i]->value);
			telmonth.month.value = 1;
			writer->emit(0, &telmonth,&cell);
		}

	}

	void finish(samson::KVWriter *writer )
	{
	}



};


} // end of namespace mob2
} // end of namespace samson

#endif
