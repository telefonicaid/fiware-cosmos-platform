
/**
 File autogenerated with samsonModuleParser. Please, edit to complete this operation
*/

#ifndef _H_SAMSON_url_benchmark_fake_servers
#define _H_SAMSON_url_benchmark_fake_servers


#include <samson/samson.h>

/***********************************************************
generator fake_servers
{
	out	system.String system.UInt  # ServerName - Category classification

	helpLine "Generate a fixed set of servers with one associated category each"
}
************************************************************/

namespace ss{
namespace url_benchmark{


	class fake_servers : public ss::Generator
	{

	public:


		void run( ss::KVWriter *writer )
		{
			ss::system::String server;
			ss::system::UInt categ;

			// Recover the number of servers from the environment
			size_t num_servers = environment->getSizeT( "url_benchmark.num_servers" ,  1000000 );
			size_t num_categories = environment->getSizeT("url_benchmark.num_categories", 100);


			for ( size_t i = 0 ; i < num_servers ; i++)
			{
				std::ostringstream o;
				o << "ServerName_" << i;
				server.value = o.str();

				categ.value = i%num_categories;

				writer->emit( 0 , &server , &categ );
			}
		}

	};


} // end of namespace ss
} // end of namespace url_benchmark

#endif
