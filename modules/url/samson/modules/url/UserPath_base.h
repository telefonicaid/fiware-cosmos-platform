
/**
 File autogenerated with samsonModuleParser, please do not edit
*/

#ifndef _H_SAMSON_url_UserPath_BASE
#define _H_SAMSON_url_UserPath_BASE


#include <samson/modules/system/String.h>
#include <samson/modules/system/UInt.h>


namespace samson{
namespace url{


	class UserPath_base : public samson::DataInstance{

	public:
	::samson::system::UInt user;
	::samson::system::String path;

	UserPath_base() : samson::DataInstance(){
	}

	~UserPath_base() {
	}

	int parse(char *data){
		int offset=0;
		{ //Parsing user
			offset += user.parse(data+offset);
		}

		{ //Parsing path
			offset += path.parse(data+offset);
		}

		return offset;
	}

	int serialize(char *data){
		int offset=0;
		{ //Serializing user
			offset += user.serialize(data+offset);
		}

		{ //Serializing path
			offset += path.serialize(data+offset);
		}

		return offset;
	}

	static inline int size(char *data){
		int offset=0;
		{ //Sizing user
			offset += ::samson::system::UInt::size(data+offset);
		}

		{ //Sizing path
			offset += ::samson::system::String::size(data+offset);
		}

		return offset;
	}

	int hash(int max_num_partitions){
		{ //Partitioning user
			return user.hash(max_num_partitions);
		}

	}

	inline static int compare(char * data1 , char *data2 , size_t *offset1 , size_t *offset2 ){
		{ // comparing user
			int tmp = ::samson::system::UInt::compare(data1,data2,offset1 , offset2);
			if( tmp != 0) return tmp;
		}   //  user compared 
		{ // comparing path
			int tmp = ::samson::system::String::compare(data1,data2,offset1 , offset2);
			if( tmp != 0) return tmp;
		}   //  path compared 
		return 0; //If everything is equal
	}

	inline static int compare( char* data1 , char* data2 )
	{
		size_t offset_1=0;
		size_t offset_2=0;
		return compare( data1 , data2 , &offset_1 , &offset_2 );
	}




	void copyFrom( UserPath_base *other ){
		{ //Copying user
			user.copyFrom(&other->user);
		}

		{ //Copying path
			path.copyFrom(&other->path);
		}

	};

	std::string str(){
		std::ostringstream o;
				{ //Texting user
			o << user.str();
		}

		o<<" ";
				{ //Texting path
			o << path.str();
		}

		o<<" ";
		return o.str();
	}

	}; //class UserPath_base

} // end of namespace samson
} // end of namespace url

#endif
