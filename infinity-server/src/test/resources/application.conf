akka {
  loglevel = INFO
}

spray.can.server {
  request-timeout = 1s
}

infinity.server {
  services {
    namenode {
      webhdfs {
        hostname = "localhost"
        port = 8008
      }
      infinity {
        hostname = "localhost"
        port = 8009
      }
    }

    datanode1 {
      webhdfs {
        hostname = "localhost"
        port = 8010
      }
      infinity {
        hostname = "localhost"
        port = 8011
      }
    }

    active = namenode
  }
}
