// Low
set url_benchmark.num_users		7
set url_benchmark.num_servers       	5
set url_benchmark.num_paths_per_server	5
set url_benchmark.num_urls_per_user	5
set url_benchmark.num_categories		4
set url_benchmark.num_hits		3

// Medium
set url_benchmark.num_users		100000
set url_benchmark.num_servers		1000
set url_benchmark.num_paths_per_server	100
set url_benchmark.num_urls_per_user	100
set url_benchmark.num_categories		10
set url_benchmark.num_hits		5

// Create the URLs and servers
url_benchmark.fake_visits_fixed urls -create -clear
url_benchmark.fake_visits urls -create -clear

url_benchmark.fake_servers servers -create -clear

url_benchmark.map_visits urls serverPath -create -clear

url_benchmark.export_generated_visits urls urls.txt -clear -create
url_benchmark.export_generated_servers servers servers.txt -clear -create
download urls.txt urls.txt
download servers.txt servers.txt

// Parse and Map URLs
add urls.txt -txt
upload urls.txt urls.txt
url_benchmark.parse_visits urls.txt serverPath -create -clear

add servers.txt -txt
upload servers.txt servers.txt
url_benchmark.parse_servers servers.txt servers -create -clear


#
# Server INFO categories problem
#

url_benchmark.join_URLs_category_per_server serverPath servers catHitsperServer -create -clear
url_benchmark.select_top_hits_per_category catHitsperServer catTopHits -create -clear
url_benchmark.export_top_hits_per_category catTopHits catTopHits.txt -create -clear
download catTopHits.txt catTopHits.txt








