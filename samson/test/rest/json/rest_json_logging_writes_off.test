--NAME--
REST JSON /samson/logging/writes/off
--SHELL--
curl -s --request POST http://localhost:${SAMSON_WORKER_WEB_PORT_ENV}/samson/logging/writes/off.json
--EXPECT--
{{"writes":"writes turned OFF"}}
