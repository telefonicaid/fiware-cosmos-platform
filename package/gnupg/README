GnuPG keys for signing packages
==============================

Installation
------------

1. Install gpg
2. Install the private/secret key:
    gpg --import samson-dev-sec.gpg
3. Install the public key:
    gpg --import samson-dev.gpg
