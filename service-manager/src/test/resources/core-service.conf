"core" {
    "properties" {
        "core.example"="core-${masternode}"
    }
}

"a-test-service" {
    "configType"="test-service-site"
    "properties" {
        "service.example"="service-${masternode}"
    }
}
