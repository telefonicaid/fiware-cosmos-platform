--NAME--
rm_queue - delete a named queue
--SHELL-INIT--
samsonWorker
--DELILAH-INIT--
remove_all_stream
push input.txt input
--SHELL--
delilah -command "rm input"
delilah -command ls
--EXPECT--
No records
