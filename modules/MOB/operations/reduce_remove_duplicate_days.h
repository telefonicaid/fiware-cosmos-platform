
/**
 File autogenerated with samsonModuleParser. Please, edit to complete this operation
 */

#ifndef _H_SAMSON_MOB_reduce_remove_duplicate_days
#define _H_SAMSON_MOB_reduce_remove_duplicate_days


#include <samson/module/samson.h>


namespace samson{
namespace MOB{


class reduce_remove_duplicate_days : public samson::Reduce
{

	samson::system::Void void_data;

public:


#ifdef INFO_COMMENT //Just to include a comment without conflicting anything
	// If interface changes and you do not recreate this file, consider updating this information (and of course, the module file)

	input: MOB.PhoneLocCounter system.Void
	output: MOB.PhoneLoc system.Void

	helpLine: Keep only different days per location.
	extendedHelp: 		Keep only different days per location.

#endif // de INFO_COMMENT

	void init(samson::KVWriter *writer )
	{
	}

	/**
	 * Remove occurrences with the same day, so that
	 * we only keep different days per location.
	 *
	 * @return Void.
	 */
	void run(  samson::KVSetStruct* inputs , samson::KVWriter *writer )
	{
		PhoneLocCounter phoneLocCounter;
		PhoneLoc phoneLoc;

		phoneLocCounter.parse( inputs[0].kvs[0]->key );

		phoneLoc.phone.value = phoneLocCounter.phone.value;
		phoneLoc.loc = phoneLocCounter.loc;
		writer->emit(0, &phoneLoc, &void_data );
	}

	void finish(samson::KVWriter *writer )
	{
	}



};


} // end of namespace samson
} // end of namespace MOB

#endif
