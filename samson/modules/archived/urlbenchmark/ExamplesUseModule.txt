// Low
set urlbenchmark.num_users		7
set urlbenchmark.num_servers       	5
set urlbenchmark.num_paths_per_server	5
set urlbenchmark.num_urls_per_user	5
set urlbenchmark.num_categories		4
set urlbenchmark.num_hits		3

// Medium
set urlbenchmark.num_users		100000
set urlbenchmark.num_servers		1000
set urlbenchmark.num_paths_per_server	100
set urlbenchmark.num_urls_per_user	100
set urlbenchmark.num_categories		10
set urlbenchmark.num_hits		5

// Create the URLs and servers
urlbenchmark.fake_visits_fixed urls -create -clear
urlbenchmark.fake_visits urls -create -clear

urlbenchmark.fake_servers servers -create -clear

urlbenchmark.map_visits urls serverPath -create -clear

urlbenchmark.export_generated_visits urls urls.txt -clear -create
urlbenchmark.export_generated_servers servers servers.txt -clear -create
download urls.txt urls.txt
download servers.txt servers.txt

// Parse and Map URLs
add urls.txt -txt
upload urls.txt urls.txt
urlbenchmark.parse_visits urls.txt serverPath -create -clear

add servers.txt -txt
upload servers.txt servers.txt
urlbenchmark.parse_servers servers.txt servers -create -clear


#
# Server INFO categories problem
#

urlbenchmark.join_URLs_category_per_server serverPath servers catHitsperServer -create -clear
urlbenchmark.select_top_hits_per_category catHitsperServer catTopHits -create -clear
urlbenchmark.export_top_hits_per_category catTopHits catTopHits.txt -create -clear
download catTopHits.txt catTopHits.txt








