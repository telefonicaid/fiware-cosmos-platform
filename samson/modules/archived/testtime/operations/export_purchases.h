
/**
 File autogenerated with samsonModuleParser. Please, edit to complete this operation
*/

#ifndef _H_SAMSON_testtime_export_purchases
#define _H_SAMSON_testtime_export_purchases


#include <samson/module/samson.h>
#include <samson/modules/system/UInt.h>
#include <samson/modules/system/UInt8.h>



namespace samson{
namespace testtime{


	class export_purchases : public samson::ParserOut
	{

#define MAX_STR_LEN 1024
		char output[MAX_STR_LEN];

	public:


		void run(KVSetStruct* inputs , TXTWriter *writer )
		{
			const char *wdays[] = {"Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"};
			samson::system::UInt8 day;
			samson::system::UInt total_amount;

			for (size_t i = 0; (i < inputs[0].num_kvs); i++)
			{
				day.parse( inputs[0].kvs[i]->key );
				total_amount.parse( inputs[0].kvs[i]->value );

				snprintf(output, MAX_STR_LEN, "%s --> %lu\n", wdays[day.value], total_amount.value);

				writer->emit(output);
			}
		}


	};


} // end of namespace samson
} // end of namespace testtime

#endif
