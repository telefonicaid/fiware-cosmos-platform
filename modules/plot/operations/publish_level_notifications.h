
/**
 File autogenerated with samsonModuleParser. Please, edit to complete this operation
*/

#ifndef _H_SAMSON_plot_publish_level_notifications
#define _H_SAMSON_plot_publish_level_notifications


#include <samson/module/samson.h>
#include <samson/modules/plot/LevelNotification.h>
#include <samson/modules/system/String.h>


namespace samson{
namespace plot{


	class publish_level_notifications : public samson::ParserOut
	{
	   samson::system::String name;
	   samson::plot::LevelNotification level_notification;

	public:


#ifdef INFO_COMMENT //Just to include a comment without conflicting anything
// If interface changes and you do not recreate this file, consider updating this information (and of course, the module file)

input: system.String plot.LevelNotification  

helpLine: Get a txt version of content
#endif // de INFO_COMMENT

		void init( TXTWriter *writer )
		{
		}

		void run( KVSetStruct* inputs , TXTWriter *writer )
		{
		   char line[1024];

		   for ( size_t i=0 ; i < inputs[0].num_kvs ; i++ )
		   {
			  name.parse( inputs[0].kvs[i]->key );
			  level_notification.parse( inputs[0].kvs[i]->value );

			  sprintf(line,"%s %15f\n" , name.value.c_str() , level_notification.level.value );			  
			  writer->emit( line );
			  
		   }
		}

		void finish( TXTWriter *writer )
		{
		}



	};


} // end of namespace plot
} // end of namespace samson

#endif
