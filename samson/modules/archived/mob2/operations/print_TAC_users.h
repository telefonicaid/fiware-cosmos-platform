
/**
 File autogenerated with samsonModuleParser. Please, edit to complete this operation
*/

#ifndef _H_SAMSON_mob2_print_TAC_users
#define _H_SAMSON_mob2_print_TAC_users


#include <samson/module/samson.h>
#include <samson/modules/system/TimeUnix.h>
#include <samson/modules/system/UInt.h>


namespace samson{
namespace mob2{


	class print_TAC_users : public samson::ParserOutReduce
	{
		//Inputs
		samson::system::UInt node;
		samson::system::UInt32 tac;

#ifdef MAX_STR_LEN
#undef MAX_STR_LEN
#endif
#define MAX_STR_LEN 1024
	char output[MAX_STR_LEN];

	public:


#ifdef INFO_COMMENT //Just to include a comment without conflicting anything
// If interface changes and you do not recreate this file, consider updating this information (and of course, the module file)

input: system.UInt32 system.UInt

extendedHelp: 		For every TAC, prints number of users

#endif // de INFO_COMMENT

		void init(TXTWriter *writer )
		{
		}

		void run(KVSetStruct* inputs , TXTWriter *writer )
		{
			int total = 0;

			if (inputs[0].num_kvs == 0)
			{
				return;
			}
			tac.parse(inputs[0].kvs[0]->key);
			total = snprintf(output, MAX_STR_LEN, "%d|%lu\n", tac.value, inputs[0].num_kvs);
			writer->emit(output);
		}

		void finish(TXTWriter *writer )
		{
		}



	};


} // end of namespace mob2
} // end of namespace samson

#endif
