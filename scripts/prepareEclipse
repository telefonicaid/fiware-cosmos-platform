mkdir -p eclipse_proj
cd eclipse_proj 2> /dev/null
if [ "$?" != 0 ]
then
	echo "You need be on trunk to build"
	exit 1
fi

export PATH=/usr/local/bin:$PATH
cmake .. -DCMAKE_BUILD_TYPE=DEBUG -G "Eclipse CDT4 - Unix Makefiles"

