
/**
 File autogenerated with samsonModuleParser. Please, edit to complete this operation
*/

#ifndef _H_SAMSON_example_reduce2
#define _H_SAMSON_example_reduce2


#include <samson/Operation.h>
#include <samson/modules/system/UInt.h>

namespace ss{
namespace example{


	class reduce2 : public ss::Reduce
	{
		ss::system::UInt key;
		ss::system::UInt value;

		ss::system::UInt total1;
		ss::system::UInt total2;
		
		
	public:


		void run(  ss::KVSetStruct* inputs , ss::KVWriter *writer )
		{
			total1.value = 0;
			for (size_t i = 0 ; i < inputs[0].num_kvs ; i++)
			{
				value.parse( inputs[0].kvs[i]->value );
				total1.value += value.value;
			}
			
			total2.value = 0;
			for (size_t i = 0 ; i < inputs[1].num_kvs ; i++)
			{
				value.parse( inputs[1].kvs[i]->value );
				total2.value += value.value;
			}
			
			writer->emit( 0 , &key, &total1 );
			writer->emit( 1 , &key, &total2 );
			
			
		}


	};


} // end of namespace ss
} // end of namespace example

#endif
