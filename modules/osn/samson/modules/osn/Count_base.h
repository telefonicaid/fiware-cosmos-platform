
/**
 File autogenerated with samsonModuleParser, please do not edit
*/

#ifndef _H_SAMSON_osn_Count_BASE
#define _H_SAMSON_osn_Count_BASE


#include <samson/modules/system/String.h>
#include <samson/modules/system/UInt.h>


namespace samson{
namespace osn{


	class Count_base : public samson::DataInstance{

	public:
	::samson::system::UInt count;
	::samson::system::String element;

	Count_base() : samson::DataInstance(){
	}

	~Count_base() {
	}

	int parse(char *data){
		int offset=0;
		{ //Parsing count
			offset += count.parse(data+offset);
		}

		{ //Parsing element
			offset += element.parse(data+offset);
		}

		return offset;
	}

	int serialize(char *data){
		int offset=0;
		{ //Serializing count
			offset += count.serialize(data+offset);
		}

		{ //Serializing element
			offset += element.serialize(data+offset);
		}

		return offset;
	}

	static inline int size(char *data){
		int offset=0;
		{ //Sizing count
			offset += ::samson::system::UInt::size(data+offset);
		}

		{ //Sizing element
			offset += ::samson::system::String::size(data+offset);
		}

		return offset;
	}

	int hash(int max_num_partitions){
		{ //Partitioning count
			return count.hash(max_num_partitions);
		}

	}

	inline static int compare(char * data1 , char *data2 , size_t *offset1 , size_t *offset2 ){
		{ // comparing count
			int tmp = ::samson::system::UInt::compare(data1,data2,offset1 , offset2);
			if( tmp != 0) return tmp;
		}   //  count compared 
		{ // comparing element
			int tmp = ::samson::system::String::compare(data1,data2,offset1 , offset2);
			if( tmp != 0) return tmp;
		}   //  element compared 
		return 0; //If everything is equal
	}

	inline static int compare( char* data1 , char* data2 )
	{
		size_t offset_1=0;
		size_t offset_2=0;
		return compare( data1 , data2 , &offset_1 , &offset_2 );
	}




	void copyFrom( Count_base *other ){
		{ //Copying count
			count.copyFrom(&other->count);
		}

		{ //Copying element
			element.copyFrom(&other->element);
		}

	};

	std::string str(){
		std::ostringstream o;
				{ //Texting count
			o << count.str();
		}

		o<<" ";
				{ //Texting element
			o << element.str();
		}

		o<<" ";
		return o.str();
	}

	}; //class Count_base

} // end of namespace samson
} // end of namespace osn

#endif
