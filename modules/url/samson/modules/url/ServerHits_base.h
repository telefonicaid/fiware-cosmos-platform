
/**
 File autogenerated with samsonModuleParser, please do not edit
*/

#ifndef _H_SAMSON_url_ServerHits_BASE
#define _H_SAMSON_url_ServerHits_BASE


#include <samson/modules/system/String.h>
#include <samson/modules/system/UInt.h>


namespace samson{
namespace url{


	class ServerHits_base : public samson::DataInstance{

	public:
	::samson::system::String server;
	::samson::system::UInt hits;

	ServerHits_base() : samson::DataInstance(){
	}

	~ServerHits_base() {
	}

	int parse(char *data){
		int offset=0;
		{ //Parsing server
			offset += server.parse(data+offset);
		}

		{ //Parsing hits
			offset += hits.parse(data+offset);
		}

		return offset;
	}

	int serialize(char *data){
		int offset=0;
		{ //Serializing server
			offset += server.serialize(data+offset);
		}

		{ //Serializing hits
			offset += hits.serialize(data+offset);
		}

		return offset;
	}

	static inline int size(char *data){
		int offset=0;
		{ //Sizing server
			offset += ::samson::system::String::size(data+offset);
		}

		{ //Sizing hits
			offset += ::samson::system::UInt::size(data+offset);
		}

		return offset;
	}

	int hash(int max_num_partitions){
		{ //Partitioning server
			return server.hash(max_num_partitions);
		}

	}

	inline static int compare(char * data1 , char *data2 , size_t *offset1 , size_t *offset2 ){
		{ // comparing server
			int tmp = ::samson::system::String::compare(data1,data2,offset1 , offset2);
			if( tmp != 0) return tmp;
		}   //  server compared 
		{ // comparing hits
			int tmp = ::samson::system::UInt::compare(data1,data2,offset1 , offset2);
			if( tmp != 0) return tmp;
		}   //  hits compared 
		return 0; //If everything is equal
	}

	inline static int compare( char* data1 , char* data2 )
	{
		size_t offset_1=0;
		size_t offset_2=0;
		return compare( data1 , data2 , &offset_1 , &offset_2 );
	}




	void copyFrom( ServerHits_base *other ){
		{ //Copying server
			server.copyFrom(&other->server);
		}

		{ //Copying hits
			hits.copyFrom(&other->hits);
		}

	};

	std::string str(){
		std::ostringstream o;
				{ //Texting server
			o << server.str();
		}

		o<<" ";
				{ //Texting hits
			o << hits.str();
		}

		o<<" ";
		return o.str();
	}

	}; //class ServerHits_base

} // end of namespace samson
} // end of namespace url

#endif
