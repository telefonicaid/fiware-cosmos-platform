
/**
 File autogenerated with samsonModuleParser. Please, edit to complete this operation
*/

#ifndef _H_SAMSON_example_map_list
#define _H_SAMSON_example_map_list


#include <samson/module/samson.h>
#include <samson/modules/example/ExampleData.h>
#include <samson/modules/system/UInt.h>


namespace samson{
namespace example{


	class map_list : public samson::Map
	{

	public:


#ifdef INFO_COMMENT //Just to include a comment without conflicting anything
// If interface changes and you do not recreate this file, consider updating this information (and of course, the module file)

input: system.UInt example.ExampleData  
output: system.UInt example.ExampleData

helpLine: Example of how to use list types
#endif // de INFO_COMMENT

		void init( samson::KVWriter *writer )
		{
		}

		void run( samson::KVSetStruct* inputs , samson::KVWriter *writer )
		{
		   samson::system::UInt key;
		   samson::example::ExampleData value;

		   for (size_t i = 0 ; i < inputs[0].num_kvs ; i++ )
		   {
			  key.parse( inputs[0].kvs[i]->key );
			  value.parse( inputs[0].kvs[i]->value );


			  if( value.list.count() > 0 )
			  {
				 //delete value.list.extract_front();
				 value.list.add_back( value.list.extract_front() );
			  }

			  writer->emit( 0 , &key ,&value );

		   }
		   
		}

		void finish( samson::KVWriter *writer )
		{
		}



	};


} // end of namespace example
} // end of namespace samson

#endif
