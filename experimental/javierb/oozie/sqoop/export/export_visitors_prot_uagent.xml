<action name="EXPORT_VISITORS_PROT_UAGENT">
	<sqoop xmlns="uri:oozie:sqoop-action:0.2">
		<job-tracker>${jobTracker}</job-tracker>
		<name-node>${nameNode}</name-node>
		<configuration>
			<property>
				<name>mapred.job.queue.name</name>
				<value>${queueName}</value>
			</property>
			<property>
				<name>oozie.use.system.libpath</name>
				<value>true</value>
			</property>

		</configuration>
		<command>export --connect jdbc:mysql://pshdp01/psdb --table ag_visitors_uagent_day_aux --username hadoop --password hadoop --jar-file /usr/lib/sqoop/lib/kpiexportation.jar --class-name ag_visitors_uagent_day_aux --export-dir ${nameNode}/user/javierb/es/webprofiling/aggregation/VISITORS_PROT_UAGENT/${outputDir}/</command>
	</sqoop>
	<ok to="exportjoining"/>
	<error to="exportjoining"/>
</action>