/**
 * CDR Protocol.
 *
 * author: dmicol
 */
@namespace("es.tid.ps.base.data")
protocol CDRProtocol {
  enum Kind {
    VOICE,
    SMS
  }

  record CDR {
    string userid_from;
    string userid_to;
    string cell;
    Kind kind;
    string length;
  }
}
