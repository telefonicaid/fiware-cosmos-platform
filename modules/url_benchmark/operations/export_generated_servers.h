
/**
 File autogenerated with samsonModuleParser. Please, edit to complete this operation
*/

#ifndef _H_SAMSON_url_benchmark_export_generated_servers
#define _H_SAMSON_url_benchmark_export_generated_servers


#include <samson/samson.h>

/******************************************************
parserOut export_generated_servers
{
	in system.String system.UInt  # ServerName - Category classification
	out txt txt

	helpLine "Dumps to text the servers classification"
}
*******************************************************/

namespace ss{
namespace url_benchmark{


	class export_generated_servers : public ss::ParserOut
	{
		ss::system::String serverName;
		ss::system::UInt categ;

#ifdef MAX_STR_LEN
#undef MAX_STR_LEN
#endif

#define MAX_STR_LEN 1024
			char output[MAX_STR_LEN];

	public:


		void run(KVSetStruct* inputs , TXTWriter *writer )
		{
			for (size_t i = 0; (i < inputs[0].num_kvs); i++)
			{
				serverName.parse(inputs[0].kvs[i]->key);
				categ.parse(inputs[0].kvs[i]->value);

				snprintf(output, MAX_STR_LEN, "%s %lu\n", serverName.value.c_str(), categ.value);

				writer->emit(output);
			}
		}


	};


} // end of namespace ss
} // end of namespace url_benchmark

#endif
