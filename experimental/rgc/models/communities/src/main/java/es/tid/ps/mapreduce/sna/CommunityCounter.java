package es.tid.ps.mapreduce.sna;

/**
 * Enum in where define the counters that we are going to monitor into the
 * cluster. <br>
 * LINE_PARSER_SOCIAL_GRAPH_ERROR: the wrong lines into the cdrs file. <br>
 * 
 * @author rgc
 */
public enum CommunityCounter {
    LINE_PARSER_SOCIAL_GRAPH_ERROR
}
