include "cluster-properties"

"global" {
    "properties" {
        "global.example.string"="global-"${MasterNode}${MaxMapTasks}${MaxReduceTasks}
        "global.example.boolean"=true
        "global.example.number"=29
    }
}

"core" {
    "properties" {
        "core.example"="core-"${MasterNode}${MaxMapTasks}${MaxReduceTasks}
    }
}

"global-core-service" {
    "configType"=test-service-site
    "properties" {
        "service.example"="service-"${MasterNode}${MaxMapTasks}${MaxReduceTasks}
    }
}
