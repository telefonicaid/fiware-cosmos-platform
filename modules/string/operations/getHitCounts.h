
/**
 File autogenerated with samsonModuleParser. Please, edit to complete this operation
*/

#ifndef _H_SAMSON_string_getHitCounts
#define _H_SAMSON_string_getHitCounts


#include <samson/module/samson.h>

#include "samson/modules/system/HitCount.h"
#include "samson/modules/system/String.h"

namespace samson{
namespace string{


	class getHitCounts : public samson::ParserOut
	{

	  samson::system::String key;
	  samson::system::HitCount value;

	public:


#ifdef INFO_COMMENT //Just to include a comment without conflicting anything
// If interface changes and you do not recreate this file, consider updating this information (and of course, the module file)

input: system.String system.HitCount  

helpLine: Just get the current hits per word
#endif // de INFO_COMMENT

		void init(TXTWriter *writer )
		{
		}

		void run(KVSetStruct* inputs , TXTWriter *writer )
		{
		  for( size_t i = 0 ; i < inputs[0].num_kvs ; i++)
		  {
		    key.parse( inputs[0].kvs[i]->key );
		    value.parse( inputs[0].kvs[i]->value );

		    char line[1024];
		    sprintf(line, "%s %lu\n" , key.value.c_str() , value.getHits() );
		      
		    writer->emit( line );
		  }
		}

		void finish(TXTWriter *writer )
		{
		}



	};


} // end of namespace samson
} // end of namespace string

#endif
