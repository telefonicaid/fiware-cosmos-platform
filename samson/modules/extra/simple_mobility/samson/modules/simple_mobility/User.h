
/**
 File autogenerated with samsonModuleParser. Please, edit to complete this data
*/

#ifndef _H_SAMSON_simple_mobility_User
#define _H_SAMSON_simple_mobility_User


#include <samson/modules/simple_mobility/User_base.h>


namespace samson{
namespace simple_mobility{


	class User : public User_base
	{

	public:

	   void init()
	   {
		  // Init the position element
		  position.set( 0 , 0 );

		  // Set length of the areas
		  areasSetLength(0);

		  // Set length of the cells
		  cellsSetLength(0);

		  // Set tracking to false
		  setTraking( false );
	   }

	   bool isTracking()
	   {
		  return (track.value != 0);
	   }
	   
	   void setTraking( bool p )
	   {
		  if ( p )
			 track.value = 1;
		  else
			 track.value = 0;
	   }

	   samson::mobility::Area* getArea( std::string name )
	   {
		  for (int i = 0 ; i < areas_length ; i++ )
			 if( areas[i].name.value == name )
				return &areas[i];

		  return NULL;
	   }

	   samson::mobility::CellIdLabelled* getCellIdLabelled( std::string name )
	   {
		  for (int i = 0 ; i < cells_length ; i++ )
			 if( cells[i].name.value == name )
				return &cells[i];

		  return NULL;
	  }

	};


} // end of namespace samson
} // end of namespace simple_mobility

#endif
