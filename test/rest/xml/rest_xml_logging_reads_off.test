--NAME--
REST XML /samson/logging/reads/off
--SHELL--
curl -s --request POST http://localhost:${SAMSON_WORKER_WEB_PORT_ENV}/samson/logging/reads/off
--EXPECT--
<?xml version="1.0" encoding="UTF-8"?>
<!-- SAMSON Rest interface -->
<samson>
<reads><![CDATA[reads turned OFF]]></reads>
</samson>
