mkdir working || true
mkdir working/modules || true
cp -f *.so working/modules

samsonLocal -working working -f ../test_1_commands.txt > test_1_stdout 2> test_1_stderr

cat out.txt | sort -n > test_1_out.txt
rm -f out.txt

diff test_1_out.txt ../test_1_expected_out.txt
