--NAME--
REST XML /samson/logging/writes/on
--SHELL--
curl -s --request POST http://localhost:${SAMSON_WORKER_WEB_PORT_ENV}/samson/logging/writes/on
--EXPECT--
<?xml version="1.0" encoding="UTF-8"?>
<!-- SAMSON Rest interface -->
<samson>
<writes><![CDATA[writes turned ON]]></writes>
</samson>
